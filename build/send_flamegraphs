#!/bin/ksh

#We pack, compress, and scp the flamegraphs and their stacks.
#arg1 is the user@machine arg.

dir=fg_stacks

if [[ ! -a fg_stacks.tar.gz ]]; then;
	tar cvf fg_stacks.tar fg_stacks
	gzip fg_stacks.tar
fi;

scp fg_stacks.tar.gz $1:fg_stacks.tar.gz
