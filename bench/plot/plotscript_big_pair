#!/bin/gnuplot

#
#
# Next, we plot our sorted structures at significantly larger input sizes. We
# no longer need the logarithmic scale, as we have dropped linked lists from
# the plots. All of these structs are on the same plot.
#
#

set terminal pbm small color size 640,480
set output outputfile
set format x "%10.1e"
plot inputfile1 using 1:2 notitle ,\
inputfile2 using 1:2 notitle
